# data-product-hack-dopamine

create virtualenv:  
`pipenv install`  

activate virtualenv:  
`pipenv shell`

create local env variables:
`cp .env.example .env`

## pre-commit hooks

to install:
`pre-commit install`

update hooks version:
`pre-commit autoupdate`

manually run hooks:
`pre-commit`